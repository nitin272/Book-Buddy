<h1>Review Details</h1>

<p><strong>Comment:</strong> <%= @review.comment %></p>
<p><strong>Rating:</strong> <%= @review.rating %></p>

<%= link_to "Edit this review", edit_book_review_path(@book, @review), class: "btn btn-secondary" %> |
<%= button_to "Delete", book_review_path(@book, @review), method: :delete, data: { confirm: "Are you sure?" }, class: "btn btn-sm btn-danger" %>

<%= link_to "Back", book_reviews_path(@book), class: "btn btn-link" %>
